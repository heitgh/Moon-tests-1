import type { WebContents } from "electron";
export class WebContentsManager { readonly #items = new Map<string, WebContents>(); register(tabId: string, contents: WebContents): void { if (this.#items.has(tabId)) throw new Error(`WebContents already registered for tab: ${tabId}`); this.#items.set(tabId, contents); contents.once("destroyed", () => this.#items.delete(tabId)); } get(tabId: string): WebContents | undefined { const value = this.#items.get(tabId); return value && !value.isDestroyed() ? value : undefined; } require(tabId: string): WebContents { const value = this.get(tabId); if (!value) throw new Error(`WebContents not found for tab: ${tabId}`); return value; } remove(tabId: string): void { const value = this.get(tabId); if (value) value.close(); this.#items.delete(tabId); } destroyAll(): void { for (const id of [...this.#items.keys()]) this.remove(id); } }
