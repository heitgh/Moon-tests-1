import type { MoonExtensionApi } from "../api/moon-extension-api.js";import type { Disposable,MoonExtensionMetadata,MoonExtensionModule } from "./types.js";
export class MoonExtensionSdk{readonly #disposables=new Set<Disposable>();constructor(readonly api:MoonExtensionApi,readonly metadata:MoonExtensionMetadata){}track(disposable:Disposable):Disposable{this.#disposables.add(disposable);return()=>{if(this.#disposables.delete(disposable))disposable();};}define(lifecycle:MoonExtensionModule["lifecycle"]):MoonExtensionModule{return{metadata:this.metadata,lifecycle};}dispose():void{for(const disposable of[...this.#disposables]){try{disposable();}finally{this.#disposables.delete(disposable);}}}}
