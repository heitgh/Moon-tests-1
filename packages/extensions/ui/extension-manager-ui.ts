import type { InstalledExtension } from "@moon/platform";
export class ExtensionManagerUi{readonly element=document.createElement("section");constructor(readonly onSelect:(id:string)=>void){}render(extensions:readonly InstalledExtension[]){const list=document.createElement("ul");for(const extension of extensions){const item=document.createElement("li");const button=document.createElement("button");button.type="button";button.textContent=`${extension.manifest.name} ${extension.manifest.version}`;button.dataset.state=extension.state;button.addEventListener("click",()=>this.onSelect(extension.id));item.append(button);list.append(item);}this.element.replaceChildren(list);}}
