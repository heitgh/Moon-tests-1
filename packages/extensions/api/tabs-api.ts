import type { BrowserPlatform,BrowserTabOptions } from "@moon/platform";import { requireExtensionPermission,type ExtensionApiContext } from "./moon-extension-api.js";
export class TabsApi{constructor(readonly context:ExtensionApiContext,readonly browser:BrowserPlatform){}get(id:string){requireExtensionPermission(this.context,"tabs");return this.browser.getTab(id);}list(windowId:string){requireExtensionPermission(this.context,"tabs");return this.browser.getTabs(windowId);}create(windowId:string,options?:BrowserTabOptions){requireExtensionPermission(this.context,"tabs");return this.browser.createTab(windowId,options);}close(id:string){requireExtensionPermission(this.context,"tabs");return this.browser.closeTab(id);}navigate(id:string,url:string){requireExtensionPermission(this.context,"tabs");return this.browser.navigate(id,url,{userInitiated:false});}executeScript(id:string,script:string){requireExtensionPermission(this.context,"scripting");return this.browser.executeScript(id,script);}}
