import type { BrowserPlatform,BrowserWindowOptions } from "@moon/platform";import { requireExtensionPermission,type ExtensionApiContext } from "./moon-extension-api.js";
export class WindowsApi{constructor(readonly context:ExtensionApiContext,readonly browser:BrowserPlatform){}create(options?:BrowserWindowOptions){requireExtensionPermission(this.context,"tabs");return this.browser.createWindow(options);}close(id:string){requireExtensionPermission(this.context,"tabs");return this.browser.closeWindow(id);}focus(id:string){requireExtensionPermission(this.context,"tabs");return this.browser.focusWindow(id);}}
