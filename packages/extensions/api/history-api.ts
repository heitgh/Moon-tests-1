import type { HistoryRepository } from "@moon/storage";import { requireExtensionPermission,type ExtensionApiContext } from "./moon-extension-api.js";
export class HistoryApi{constructor(readonly context:ExtensionApiContext,readonly repository:HistoryRepository){}recent(limit=100){requireExtensionPermission(this.context,"history");return this.repository.recent(limit);}search(query:string){requireExtensionPermission(this.context,"history");return this.repository.search(query);}delete(id:string){requireExtensionPermission(this.context,"history");return this.repository.delete(id);}}
