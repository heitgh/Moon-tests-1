import type { ExtensionPlatform,InstalledExtension } from "@moon/platform";import type { CatalogExtension } from "./extension-catalog.js";
export interface ExtensionPackageDownloader{download(url:string,checksum:string,signal?:AbortSignal):Promise<string>;remove(path:string):Promise<void>;}
export class ExtensionInstaller{constructor(readonly platform:ExtensionPlatform,readonly downloader:ExtensionPackageDownloader){}async install(item:CatalogExtension,signal?:AbortSignal):Promise<InstalledExtension>{if(!item.verified)throw new Error(`Extension is not verified: ${item.id}`);const path=await this.downloader.download(item.packageUrl,item.checksum,signal);try{return await this.platform.install(path);}finally{await this.downloader.remove(path);}}uninstall(id:string){return this.platform.uninstall(id);}}
