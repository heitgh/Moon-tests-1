import { rankOmniboxActions, type OmniboxAction } from "./omnibox-actions.js";
export interface OmniboxProvider { readonly id: string; readonly priority: number; search(query: string, signal?: AbortSignal): Promise<readonly OmniboxAction[]>; }
export class OmniboxEngine { readonly #providers = new Map<string, OmniboxProvider>(); register(provider: OmniboxProvider): () => void { this.#providers.set(provider.id, provider); return () => this.#providers.delete(provider.id); } async search(query: string, signal?: AbortSignal): Promise<readonly OmniboxAction[]> { const providers = [...this.#providers.values()].sort((a,b) => b.priority-a.priority); const results = await Promise.all(providers.map(provider => provider.search(query, signal).catch(() => []))); if (signal?.aborted) return []; return rankOmniboxActions(results.flat()); } }
