import type { HistoryModel } from "@moon/core";import type { ContextRequest,ContextSource } from "../engine/context-engine.js";
export class HistoryContextSource implements ContextSource{readonly id="history";readonly types=["history"] as const;constructor(readonly load:(limit:number)=>Promise<readonly HistoryModel[]>){}async collect(request:ContextRequest){return(await this.load(request.limit??100)).filter(item=>!request.workspaceId||item.workspaceId===request.workspaceId).map(item=>({id:item.id,type:"history" as const,title:item.title,url:item.url,timestamp:item.lastVisitedAt,relevance:Math.min(75,20+item.visitCount),metadata:{visitCount:item.visitCount}}));}}
