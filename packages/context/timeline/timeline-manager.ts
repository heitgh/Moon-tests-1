import { TimelineEngine } from "./timeline-engine.js";import type { TimelineEvent } from "./timeline-event.js";import type { TimelineQuery } from "./timeline-query.js";
export interface TimelineStore{append(event:TimelineEvent):Promise<void>;list():Promise<readonly TimelineEvent[]>;removeBefore(timestamp:number):Promise<number>;}
export class TimelineManager{constructor(readonly store:TimelineStore,readonly engine=new TimelineEngine()){}async record<T>(event:Omit<TimelineEvent<T>,"id"|"timestamp">):Promise<TimelineEvent<T>>{const value={...event,id:`timeline-${Date.now()}-${Math.random().toString(36).slice(2)}`,timestamp:Date.now()};if(!value.private)await this.store.append(value);return value;}async query(query:TimelineQuery){return this.engine.filter(await this.store.list(),query);}prune(before:number){return this.store.removeBefore(before);}}
