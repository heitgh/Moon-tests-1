import type { IntelligenceContextFragment } from "./context-source.js";
export interface BuiltContext{readonly content:string;readonly fragments:readonly IntelligenceContextFragment[];readonly tokensEstimate:number;readonly truncated:boolean;}
export class ContextBuilder{build(fragments:readonly IntelligenceContextFragment[],maxTokens:number):BuiltContext{const selected:IntelligenceContextFragment[]=[];let tokens=0;for(const fragment of[...fragments].sort((a,b)=>b.relevance-a.relevance)){if(tokens+fragment.tokensEstimate>maxTokens)continue;selected.push(fragment);tokens+=fragment.tokensEstimate;}return{content:selected.map(item=>`<context type="${item.kind}" id="${item.id}">\n${item.content}\n</context>`).join("\n\n"),fragments:selected,tokensEstimate:tokens,truncated:selected.length<fragments.length};}}
