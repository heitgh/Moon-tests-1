import type { ClipboardContent, ClipboardFormat, ClipboardPlatform } from "../interfaces/clipboard-platform.js";
export class ClipboardService { constructor(readonly platform: ClipboardPlatform) {} readText() { return this.platform.readText(); } writeText(text: string) { return this.platform.writeText(text); } read(format: ClipboardFormat): Promise<ClipboardContent | null> { return this.platform.read(format); } write(content: ClipboardContent) { return this.platform.write(content); } has(format: ClipboardFormat) { return this.platform.has(format); } clear() { return this.platform.clear(); } }
