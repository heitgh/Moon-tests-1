import type { PanelApi,PluginPanelDefinition } from "./panel-api.js";
export class PanelManager implements PanelApi{readonly #panels=new Map<string,PluginPanelDefinition>();readonly #containers=new Map<string,HTMLElement>();register(panel:PluginPanelDefinition):()=>void{if(this.#panels.has(panel.id))throw new Error(`Plugin panel already exists: ${panel.id}`);this.#panels.set(panel.id,panel);return()=>{this.hide(panel.id);this.#panels.delete(panel.id);};}async show(id:string):Promise<void>{const panel=this.#panels.get(id);if(!panel)throw new Error(`Plugin panel not found: ${id}`);const container=this.#containers.get(id)??document.createElement("section");container.className="plugin-panel";container.dataset.location=panel.location;container.setAttribute("aria-label",panel.title);this.#containers.set(id,container);if(!container.isConnected)document.body.append(container);container.hidden=false;container.replaceChildren();await panel.render(container);}hide(id:string):void{const container=this.#containers.get(id);if(container)container.hidden=true;}}
