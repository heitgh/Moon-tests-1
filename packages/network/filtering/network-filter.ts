import type { NetworkFilter as PlatformNetworkFilter,NetworkRequest,NetworkRuleDecision } from "@moon/platform";import { evaluateRequestPolicy,type RequestPolicyRule } from "./request-policy.js";
export class MoonNetworkFilter implements PlatformNetworkFilter{readonly id:string;readonly name:string;enabled:boolean;priority:number;#rules:readonly RequestPolicyRule[];constructor(options:{id:string;name:string;enabled?:boolean;priority?:number;rules?:readonly RequestPolicyRule[]}){this.id=options.id;this.name=options.name;this.enabled=options.enabled??true;this.priority=options.priority??0;this.#rules=options.rules??[];}setRules(rules:readonly RequestPolicyRule[]){this.#rules=[...rules].sort((a,b)=>b.priority-a.priority);}async evaluate(request:NetworkRequest):Promise<NetworkRuleDecision>{for(const rule of this.#rules){const result=evaluateRequestPolicy(rule,request);if(result)return result;}return{action:"allow"};}}
