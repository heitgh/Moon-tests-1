import type { Capability } from "./capability.js";import type { Permission,PermissionSubjectType } from "./permission.js";import { resolvePermission,type PermissionPolicy } from "./permission-policy.js";
export class PermissionManager{readonly #items=new Map<string,Permission>();constructor(readonly policy:PermissionPolicy){}set(input:Omit<Permission,"id"|"createdAt"|"updatedAt">):Permission{const now=Date.now();const id=`permission-${now}-${this.#items.size+1}`;const value={...input,id,createdAt:now,updatedAt:now};this.#items.set(id,value);return value;}remove(id:string){return this.#items.delete(id);}list(subjectId?:string){return[...this.#items.values()].filter(item=>!subjectId||item.subjectId===subjectId);}resolve(subjectType:PermissionSubjectType,subjectId:string,capability:Capability,scope?:string){return resolvePermission(this.policy,this.list(subjectId),subjectType,subjectId,capability,scope);}clear(subjectId?:string):void{for(const[id,item]of this.#items)if(!subjectId||item.subjectId===subjectId)this.#items.delete(id);}}
