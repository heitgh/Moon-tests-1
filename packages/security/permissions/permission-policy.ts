import type { Capability } from "./capability.js";import type { Permission,PermissionSubjectType } from "./permission.js";
export interface PermissionPolicy{readonly defaults:Readonly<Partial<Record<Capability,"allow"|"deny"|"ask">>>;readonly denyUnknown:boolean;}
export function resolvePermission(policy:PermissionPolicy,permissions:readonly Permission[],subjectType:PermissionSubjectType,subjectId:string,capability:Capability,scope?:string):"allow"|"deny"|"ask"{const now=Date.now();const match=[...permissions].reverse().find(item=>item.subjectType===subjectType&&item.subjectId===subjectId&&item.capability===capability&&(!item.scope||item.scope===scope)&&(!item.expiresAt||item.expiresAt>now));return match?.decision??policy.defaults[capability]??(policy.denyUnknown?"deny":"ask");}
