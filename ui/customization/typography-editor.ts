export interface TypographySettings { readonly family: string; readonly scale: number; readonly lineHeight: number; }
export class TypographyEditor { readonly element = document.createElement("form"); constructor(readonly onChange: (value: TypographySettings) => void) { this.element.innerHTML = `<label>Fonte<input name="family" value="Inter, system-ui"></label><label>Escala<input name="scale" type="range" min="80" max="130" value="100"></label><label>Altura de linha<input name="lineHeight" type="range" min="120" max="200" value="150"></label>`; this.element.addEventListener("input", () => { const data = new FormData(this.element); this.onChange({ family: String(data.get("family")), scale: Number(data.get("scale"))/100, lineHeight: Number(data.get("lineHeight"))/100 }); }); } }
