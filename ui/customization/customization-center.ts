export interface CustomizationSection { readonly id: string; readonly title: string; readonly element: HTMLElement; }
export class CustomizationCenter { readonly element = document.createElement("section"); readonly #sections = new Map<string, CustomizationSection>(); constructor() { this.element.className = "customization-center"; } register(section: CustomizationSection): void { this.#sections.set(section.id, section); } show(id: string): void { const section = this.#sections.get(id); if (!section) throw new Error(`Customization section not found: ${id}`); const title = document.createElement("h2"); title.textContent = section.title; this.element.replaceChildren(title, section.element); } }
