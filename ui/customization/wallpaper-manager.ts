export interface WallpaperOption { readonly id: string; readonly name: string; readonly source: string; readonly type: "image" | "gradient" | "color"; }
export class WallpaperManager { readonly #items = new Map<string, WallpaperOption>(); register(item: WallpaperOption): void { this.#items.set(item.id, item); } list(): readonly WallpaperOption[] { return [...this.#items.values()]; } apply(id: string, target: HTMLElement = document.documentElement): void { const item = this.#items.get(id); if (!item) throw new Error(`Wallpaper not found: ${id}`); target.style.backgroundImage = item.type === "image" ? `url(${JSON.stringify(item.source)})` : item.type === "gradient" ? item.source : "none"; target.style.backgroundColor = item.type === "color" ? item.source : ""; target.dataset.wallpaper = id; } }
