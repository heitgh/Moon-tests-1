export interface EffectsSettings { readonly blur: number; readonly opacity: number; readonly animations: boolean; }
export class EffectsEditor { readonly element = document.createElement("form"); constructor(readonly onChange: (settings: EffectsSettings) => void) { this.element.innerHTML = `<label>Desfoque<input name="blur" type="range" min="0" max="40" value="16"></label><label>Opacidade<input name="opacity" type="range" min="50" max="100" value="95"></label><label><input name="animations" type="checkbox" checked>Animações</label>`; this.element.addEventListener("input", () => { const data = new FormData(this.element); this.onChange({ blur: Number(data.get("blur")), opacity: Number(data.get("opacity")) / 100, animations: data.has("animations") }); }); } }
