export interface CustomizableModule { readonly id: string; readonly label: string; readonly enabled: boolean; }
export class ModuleEditor { readonly element = document.createElement("fieldset"); constructor(readonly onChange: (id: string, enabled: boolean) => void) {} render(modules: readonly CustomizableModule[]): void { this.element.replaceChildren(...modules.map(module => { const label = document.createElement("label"); const input = document.createElement("input"); input.type = "checkbox"; input.checked = module.enabled; input.addEventListener("change", () => this.onChange(module.id, input.checked)); label.append(input, document.createTextNode(module.label)); return label; })); } }
