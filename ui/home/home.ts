import { HomeLayout } from "./home-layout.js"; import { HomeStateStore } from "./home-state.js"; import { HomeWidgetRegistry } from "./home-widgets.js";
export class HomeView { readonly element = document.createElement("div"); readonly layout = new HomeLayout(); constructor(readonly widgets: HomeWidgetRegistry, readonly state: HomeStateStore) { this.element.className = "moon-home"; const heading = document.createElement("h1"); heading.textContent = state.state.greeting ?? "Bem-vindo ao Moon"; this.element.append(heading, this.layout.element); } render(): void { this.layout.render(this.state.state.widgets, new Map(this.widgets.list().map(widget => [widget.id, widget.element]))); } }
