import type { HomeWidget } from "../home-widgets.js";
export function createSearchWidget(onSearch: (query: string) => void): HomeWidget { const element = document.createElement("form"); element.className = "home-widget home-widget--search"; const input = document.createElement("input"); input.type = "search"; input.placeholder = "Pesquise ou digite um endereço"; input.setAttribute("aria-label", input.placeholder); element.append(input); element.addEventListener("submit", event => { event.preventDefault(); const query = input.value.trim(); if (query) onSearch(query); }); return { id: "search", title: "Pesquisa", element }; }
