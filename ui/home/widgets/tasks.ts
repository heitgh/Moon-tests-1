import type { HomeWidget } from "../home-widgets.js";
export interface TaskSummary { readonly id: string; readonly title: string; readonly completed: boolean; }
export function createTasksWidget(load: () => Promise<readonly TaskSummary[]>, toggle: (id: string, completed: boolean) => Promise<void>): HomeWidget { const element = document.createElement("section"); element.className = "home-widget home-widget--tasks"; const refresh = async () => { const items = await load(); element.replaceChildren(...items.map(item => { const label = document.createElement("label"); const input = document.createElement("input"); input.type = "checkbox"; input.checked = item.completed; input.addEventListener("change", () => void toggle(item.id, input.checked)); label.append(input, document.createTextNode(item.title)); return label; })); }; return { id: "tasks", title: "Tarefas", element, refresh }; }
