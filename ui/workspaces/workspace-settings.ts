export interface WorkspaceSettingsValue { readonly name: string; readonly description: string; readonly color: string; readonly layout: "standard" | "compact" | "focus"; }
export class WorkspaceSettings { readonly element = document.createElement("form"); constructor(readonly onSave: (value: WorkspaceSettingsValue) => Promise<void>) { this.element.className = "workspace-settings"; this.element.innerHTML = `<label>Nome<input name="name" required></label><label>Descrição<textarea name="description"></textarea></label><label>Cor<input name="color" type="color"></label><label>Layout<select name="layout"><option value="standard">Padrão</option><option value="compact">Compacto</option><option value="focus">Foco</option></select></label><button type="submit">Salvar</button>`; this.element.addEventListener("submit", event => { event.preventDefault(); const data = new FormData(this.element); void this.onSave({ name: String(data.get("name") ?? "").trim(), description: String(data.get("description") ?? ""), color: String(data.get("color") ?? "#7c5cff"), layout: String(data.get("layout")) as WorkspaceSettingsValue["layout"] }); }); } setValue(value: WorkspaceSettingsValue): void { for (const [key, fieldValue] of Object.entries(value)) { const field = this.element.elements.namedItem(key); if (field instanceof HTMLInputElement || field instanceof HTMLTextAreaElement || field instanceof HTMLSelectElement) field.value = fieldValue; } } }
