export class WorkspaceView { readonly element = document.createElement("section"); readonly header = document.createElement("header"); readonly content = document.createElement("div"); constructor() { this.element.className = "workspace-view"; this.content.className = "workspace-view__content"; this.element.append(this.header, this.content); } render(name: string, content: Node): void { const title = document.createElement("h1"); title.textContent = name; this.header.replaceChildren(title); this.content.replaceChildren(content); } }
