import { Sidebar } from "./sidebar.js"; import { SidebarStateStore } from "./sidebar-state.js";
export class SidebarManager { constructor(readonly sidebar: Sidebar, readonly state: SidebarStateStore) {} open(panelId?: string): void { this.state.update({ open: true, activePanel: panelId ?? this.state.state.activePanel }); this.sidebar.element.hidden = false; if (panelId) this.sidebar.show(panelId); } close(): void { this.state.update({ open: false }); this.sidebar.element.hidden = true; } toggle(panelId?: string): void { this.state.state.open ? this.close() : this.open(panelId); } resize(width: number): void { const state = this.state.update({ width }); this.sidebar.element.style.width = `${state.width}px`; } }
