export class SidebarContainer { readonly element = document.createElement("aside"); #open = true; constructor() { this.element.className = "moon-sidebar-container"; this.element.setAttribute("aria-label", "Barra lateral"); } get open() { return this.#open; } setOpen(open: boolean): void { this.#open = open; this.element.hidden = !open; this.element.setAttribute("aria-hidden", String(!open)); } toggle(): void { this.setOpen(!this.#open); } setContent(content: Node): void { this.element.replaceChildren(content); } }
