export class Titlebar { readonly element = document.createElement("header"); readonly title = document.createElement("span"); constructor() { this.element.className = "moon-titlebar"; this.element.setAttribute("data-electron-drag", "true"); this.title.className = "moon-titlebar__title"; this.title.textContent = "Moon"; this.element.append(this.title); } setTitle(value: string): void { this.title.textContent = value || "Moon"; document.title = value ? `${value} — Moon` : "Moon"; } }
