export class ContentContainer { readonly element = document.createElement("main"); constructor() { this.element.className = "moon-content"; this.element.id = "moon-content"; this.element.tabIndex = -1; } show(view: Node): void { this.element.replaceChildren(view); this.element.focus({ preventScroll: true }); } setLoading(loading: boolean): void { this.element.toggleAttribute("aria-busy", loading); } }
