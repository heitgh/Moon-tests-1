export interface ToolbarAction { readonly id: string; readonly label: string; readonly icon?: string; readonly disabled?: boolean; readonly onActivate: () => void; }
export class Toolbar { readonly element = document.createElement("nav"); constructor() { this.element.className = "moon-toolbar"; this.element.setAttribute("aria-label", "Navegação do navegador"); } setActions(actions: readonly ToolbarAction[]): void { this.element.replaceChildren(...actions.map(action => { const button = document.createElement("button"); button.type = "button"; button.dataset.action = action.id; button.disabled = action.disabled ?? false; button.setAttribute("aria-label", action.label); button.textContent = action.icon ?? action.label; button.addEventListener("click", action.onActivate); return button; })); } }
