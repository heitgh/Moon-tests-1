import { ContentContainer } from "./content-container.js"; import { SidebarContainer } from "./sidebar-container.js"; import { Titlebar } from "./titlebar.js"; import { Toolbar } from "./toolbar.js";
export class BrowserShell { readonly element = document.createElement("div"); readonly titlebar = new Titlebar(); readonly toolbar = new Toolbar(); readonly sidebar = new SidebarContainer(); readonly content = new ContentContainer(); constructor() { this.element.className = "moon-shell"; const body = document.createElement("div"); body.className = "moon-shell__body"; body.append(this.sidebar.element, this.content.element); this.element.append(this.titlebar.element, this.toolbar.element, body); } mount(target: HTMLElement): void { target.replaceChildren(this.element); } }
